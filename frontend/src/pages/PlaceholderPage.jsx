import SectionHeader from '../components/ui/SectionHeader.jsx';

function PlaceholderPage({ title }) {
  return (
    <SectionHeader
      description="Placeholder route prepared for a future PrepFlow page."
      title={title}
    />
  );
}

export default PlaceholderPage;
